# Volitional Silence Requirements
#
# Install with: pip install -r requirements.txt

# Core dependencies
torch>=2.0.0
transformers>=4.30.0

# For LoRA version
peft>=0.5.0

# Optional but recommended
accelerate>=0.20.0  # For better device management
